services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env

  # uncomment at Milestone 9
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"